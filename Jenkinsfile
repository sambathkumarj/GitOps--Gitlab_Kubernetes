pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'Basic running test...'
            }
        }


        stage("test") {
            steps {    
                    echo 'Building test...' 
            }
        }

        stage("deploy") {
            steps {
                echo 'Running deploying server...'
            }
        }
    }
}
